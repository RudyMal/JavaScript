"use strict";

// déclaration et initialisation d'un tableau avec 3 éléments
var myArray = [123, 42, 3.14, "Hello"];

// si le tableau contient au moins un élément,
// afficher le premier élément d'un tableaux
if (myArray.length > 0) {
  console.log(myArray[0]);
}

// si le tableau contient au moins un élément,
// afficher le dernier élément d'un tableaux
if (myArray.length > 0) {
console.log(myArray[myArray.length - 1]);
}
